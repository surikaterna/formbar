---
"@formbar/core": patch
---

Track private scoped capture ownership across the first successful issue-only detachment while rejecting stale writes, failed publication, and external mutations to the captured baseline.
