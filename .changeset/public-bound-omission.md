---
"@formbar/from-schema": minor
"@formbar/react-schema": minor
---

Enable validated definition-bound omit-inactive submission through prepared schema factories and forward the opt-in from useSchemaForm. Generated definitions accept a serialized submission policy and field-ID include-hidden overrides; default full-draft submission is unchanged.
