---
"@formbar/from-schema": minor
"@formbar/react-schema": patch
---

Add a renderer-neutral form presentation model with resolved field state, visible-layout pruning, issue ownership, prepared options, and deterministic field IDs.

Back React schema compatibility helpers with the shared field-state, layout-pruning, and presentation-ID primitives.
